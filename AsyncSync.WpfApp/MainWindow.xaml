<Window x:Class="AsyncSync.WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AsyncSync.WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="400">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Button x:Name="ClickAsync" Content="Async" Click="ClickAsync_Click"/>
        <Button Grid.Row="1" x:Name="ClickSync" Content="Sync" Click="ClickSync_Click" Margin="0 5 0 0"/>
        <Button Grid.Row="2" x:Name="ClickAsyncTask" Content="AsyncTask" Click="ClickAsyncTask_Click" Margin="0 5 0 0"/>
        <Button Grid.Row="3" x:Name="ClickAsyncTaskDelay" Content="AsyncTaskDelay" Click="ClickAsyncTaskDelay_Click" Margin="0 5 0 0"/>
        <Label Name="mylabel" Content="Czas" Grid.Row="4"/>

    </Grid>
</Window>
